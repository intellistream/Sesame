#include "Modular.hpp"

/**
 * @brief Construct the specific algorithm.
 */
Algorithm::Algorithm(AlgorithmName algo) {
  //  WindowModel window;
  //  DBScan db_scan;

  switch (algo) {
  case KMeans: {
    // construct the algorithm here..
  }
  }
}
